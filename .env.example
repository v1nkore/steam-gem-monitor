# Скопируй в .env для локального теста:  cp .env.example .env
# Локально можно запускать без токена в режиме DRY_RUN=1 (ничего не шлёт, только печатает).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
DRY_RUN=1
